# ЕГЭ информатика — задания 19–27

## 19–21. Теория игр
Определи выигрышные и проигрышные позиции.
    def win(x, target, moves):
        if x >= target:
            return True
        return any(not win(x + m, target, moves) for m in moves)
Точно соблюдай условие: кто ходит первым, сколько ходов и что считается победой.

## 22. Многопроцессорные системы
1. Найди процессы без зависимостей.
2. Определи время их окончания.
3. Передай время зависимым процессам.
4. Повторяй до конца.

## 23. Анализ графов
Для DAG можно считать количество путей динамикой.
    dp = [0] * n
    dp[start] = 1
    for v in order:
        for u in graph[v]:
            dp[u] += dp[v]
Кратчайший путь в невзвешенном графе — BFS.

## 24. Строки — максимальная непрерывная последовательность
    best = cur = 0
    for ch in s:
        if ...:
            cur += 1
            best = max(best, cur)
        else:
            cur = 0
    print(best)

## 25. Маски чисел / делители
    from fnmatch import fnmatch
    for n in range(1, 10**7):
        if fnmatch(str(n), '12*34'):
            print(n)

Для делителей:
    d = 1
    while d * d <= n:
        if n % d == 0:
            ...
        d += 1

## 26. Файлы / жадные алгоритмы
    with open('data.txt') as f:
        a = list(map(int, f))
    a.sort()
    ans = 0
    for x in a:
        if ...:
            ans += x
    print(ans)

## 27. Большие данные / кластеры
Стремись к O(N) или O(N log N). Избегай O(N²) при больших N.
    best = ...
    for x in data:
        ...
    print(answer1, answer2)

## Проверка
- Границы диапазона.
- Нули и отрицательные значения.
- Биты и байты.
- Сложность алгоритма.
- Формат вывода.