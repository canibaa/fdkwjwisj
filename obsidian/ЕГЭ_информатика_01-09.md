# ЕГЭ информатика — задания 1–9

## 1. Графы и таблицы
Степень вершины = количество рёбер, выходящих из вершины.
Путь = последовательность вершин, соединённых рёбрами.
Для короткого/длинного пути суммируй веса рёбер и сравнивай варианты.

## 2. Таблицы истинности
1. Запиши выражение.
2. Выпиши переменные.
3. Составь промежуточные столбцы.
4. Сопоставь фрагмент таблицы.

    from itertools import product
    for a, b, c in product([0, 1], repeat=3):
        F = ...
        print(a, b, c, F)

## 3. Базы данных
Ищи строки, удовлетворяющие условиям.

    total = 0
    for row in rows:
        if ...:
            total += row[...]
    print(total)

## 4. Кодирование / Фано
Код одного слова не должен быть началом другого.

## 5. Системы счисления
    bin(n)       # двоичная
    oct(n)       # восьмеричная
    hex(n)       # шестнадцатеричная
    int(s, base) # из системы счисления

## 6. Исполнители и Черепаха
    from turtle import *
    for _ in range(...):
        forward(...)
        right(...)

## 7. Объём информации
I = k · i · f · t — звук.
N = 2^i — количество цветов.
I = X · Y · i — графика.
I = v · t — передача.
Следи за битами/байтами и единицами времени.

## 8. Комбинаторика
    from itertools import product
    for word in product('ABCDEF', repeat=5):
        s = ''.join(word)
        if ...:
            ...

## 9. Электронные таблицы
Проверяй абсолютные ссылки $A$1, относительные ссылки A1, диапазоны и порядок действий.
Частые функции: СУММ, СРЗНАЧ, ЕСЛИ.